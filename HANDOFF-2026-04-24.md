# Session handoff — 2026-04-24 (laptop migration pause)

This file is the single entry point for picking up silicritter work on
the second laptop. It supersedes nothing; the original project handoff
at `SESSION-HANDOFF.md` is still the origin-of-project context. This
file covers what happened on 2026-04-24 and what's on deck next.

## Current state in one sentence

**Paused pending laptop migration.** Two overnight batches ran today;
the second one exposed a calibration problem (N=5 can lie by ~7×) that
forced a retrospective audit and a plan to revalidate load-bearing
claims at N=100–500 on the second laptop's better GPU.

## What happened today (2026-04-24)

Two batches ran, in this order:

1. **First batch (blocks 1–5)**: the overnight plan inherited from the
   previous session. Completed in ~1.5h wall-time (much faster than the
   5.5h plan estimate — warm JIT). Block 4 failed mid-batch with a
   tuple-subtraction bug (`_measure_fitness` returns a 3-tuple; block 4
   was subtracting tuples). Fix landed as commit `10c8951` and block 4
   was re-run cleanly.

2. **Second batch (blocks 6–8)**: designed to answer three follow-up
   questions the first batch raised:
   - Block 6 (strong-op confirm, rate=3.0, ivm=1.8, N=20): structural
     adds **nothing** over pure STDP even at the strong operating point.
   - Block 7 (500k-step structural-vs-STDP, N=20): the **+25%
     structural advantage observed at N=5 in block 3 collapsed to
     z=0.53 null at N=20**. This is the calibration finding of the day.
   - Block 8 (STDP regression bisect, 100k→200k, N=20): the regression
     is real and statistically solid — fitness degrades monotonically
     past 100k steps (−4σ by 200k).

All 965 runs across both batches are pushed to GitHub.

## The block-7 calibration event

This is the load-bearing finding to internalize before doing anything
else:

- Block 3 (N=5) reported structural-at-500k beat STDP-only by +25% at
  +1.01e-04 vs +7.96e-05. Looked solid at the time.
- Block 7 (N=20) reports +3.6% effect (+7.75e-05 vs +7.47e-05), z=0.53
  — clearly null.
- **The N=5 point estimate was ~7× larger than the truth.** Not wider
  CIs around the same number — just wrong.

Implication: any prior claim in steps 1–17 measured at N<20 is suspect.
Most of the project's headline numbers are N=1 or N=5.

## The risk graph (from the retrospective audit)

```
step 10 (-5.60e-5 hand-wired closed-loop breakthrough)
├── step 11 CPPN "+12% over hand-wired" (N=1, seed=0 only)
├── step 12 "tonic cliff at 4 mV" (relative to step 10)
├── step 13 E/I grid (N=1 scan; N=5 multi-seed for the winner)
├── step 14 fGn tracking (N=1 per H)
├── step 15 "1.9–2.4× above WK floor" (analytic ratio, numerator is step-10)
├── step 16 "STDP +14%, bounded by topology" (N=1; topology == step 10)
└── README headline "closed-loop adrenaline breaks the static ceiling"
```

Step 10 is the root. Almost every subsequent claim inherits its N.

## What to do first on the second laptop

**1. Raster-retention OOM fix (blocking).** `_training_scan` in
`experiments/step17_structural_growth.py` (and its step 16 counterpart)
returns full `(n_steps × n_neurons × 2)` spike rasters that aren't used
downstream — only `val_trace` summary and the final pool state matter.
At 1M steps that's 512 MB, at 10M it's 5 GB — hits GPU OOM today at
1M on the current laptop. Fix is small: return only what's used
(`final_state`, `val_trace`, and per-step `active_trace` if still
needed) and drop the rasters. Do this before any revalidation batch.

**2. Revalidation batch (the point of the second-laptop move).** After
the OOM fix, build a single batch covering the load-bearing claims from
the risk graph, at **N=100 minimum, N=500 for headline numbers**, and
**training-duration sweeps 10k → 10M**. Sketch:

| Block | Target | N | Notes |
|---|---|---|---|
| 9 | step 10 hand-wired closed-loop | 500 | Root of risk graph. Sweep training durations 10k–10M. |
| 10 | step 13 E/I (0.2, 8.0) vs (0.2, 4.0) | 100 | Was the prior "multi-seed discipline" flagship at N=5. |
| 11 | step 11 CPPN GA topology discovery | 20 GAs × 100 eval seeds | Tests whether 88/12 cross/recurrent split is stable. Most compute-heavy block. |
| 12 | step 14 fGn H ∈ {0.3, 0.5, 0.7, 0.9} | 100 per H | Validates non-monotonic H-dependence. |
| 13 | step 16 STDP +14% | 100 | Partially confirmed via blocks 4/5 already; formalize. |

Measure **absolute deltas**, not ratios-to-step-10. That way when
block 9 lands the true step-10 number, every derived ratio (step-15
WK-floor 1.9–2.4×, step-11 +12%, etc.) can be recomputed without
re-running.

Wall-time estimate on current GPU was ~17 hours. Second-laptop GPU is
reportedly better, so probably faster. Let it run — days of unattended
compute is fine per Ed's stated constraint.

**3. DECISIONS.md D010 entry.** Codify the N ≥ 100 discipline for
load-bearing claims, citing block 7 as the calibration event that
motivated it. Retrofit the README and `perf_history.md` with
"replication pending" flags on affected claims.

## What lives where (files to migrate)

**Inside the repo (`git clone git@github.com:edhodapp/silicritter.git`
on the new laptop does it all):**
- Source (`src/`, `tests/`, `experiments/`)
- All CSVs and logs (`overnight_results/`)
- Docs (`README.md`, `DECISIONS.md`, `perf_history.md`, `CLAUDE.md`,
  `SESSION-HANDOFF.md`, this file)
- License (`LICENSE`, `COPYRIGHT`)

**Outside the repo (copy manually via rsync / scp — details below):**

| Path | Purpose | Priority |
|---|---|---|
| `~/.claude/projects/-home-ed-silicritter/memory/` | Claude Code per-project memory for silicritter. Contains audit conclusions, revalidation plan, and feedback from today. **Critical** — tomorrow-Claude needs these. | **Critical** |
| `~/.claude/CLAUDE.md` | Global Claude Code instructions for all projects. | Critical |
| `~/.claude/pylintrc` | Google Python Style Guide config. Referenced by global CLAUDE.md. | Critical |
| `~/.claude/settings.json` | Global Claude Code settings (NOT `settings.local.json` — that's per-machine). | High |
| `~/.claude/*.pdf` | FSA paper and Lextrait abstraction paper, both referenced in global CLAUDE.md. | Medium |
| `~/tools/code-review/` | Pre-commit pipeline scripts (Gemini review, quality gates). Shared across projects. | Critical |
| `~/.gemini/` | Gemini CLI auth state (`oauth_creds.json` is sensitive; rsync over trusted LAN is fine). | Critical |

**Needs recreation, not copied:**
- Python 3.12 venv — `python3.12 -m venv .venv && source .venv/bin/activate && pip install -e . && pip install jax[cuda12]` (match the JAX[cuda] wheel to whatever CUDA/driver the second laptop has).
- `.git/hooks/pre-commit` symlink — `ln -sf ~/tools/code-review/pre-commit-hook.sh .git/hooks/pre-commit`.

## WiFi-migration commands

This laptop's IP (as of now): `192.168.1.38`. Run the following **from
the second laptop** (pulls from this one). Replace `ed@192.168.1.38`
with this laptop's hostname or a different IP if the LAN reassigns.

```bash
# Clone the repo (covers everything inside silicritter/).
git clone git@github.com:edhodapp/silicritter.git

# Memory for Claude Code's silicritter project (critical).
mkdir -p ~/.claude/projects/-home-ed-silicritter
rsync -av ed@192.168.1.38:/home/ed/.claude/projects/-home-ed-silicritter/memory \
    ~/.claude/projects/-home-ed-silicritter/

# Global Claude Code config + docs.
rsync -av \
    ed@192.168.1.38:/home/ed/.claude/CLAUDE.md \
    ed@192.168.1.38:/home/ed/.claude/pylintrc \
    ed@192.168.1.38:/home/ed/.claude/settings.json \
    ~/.claude/
rsync -av \
    'ed@192.168.1.38:/home/ed/.claude/*.pdf' \
    ~/.claude/

# Shared code-review tooling (pre-commit pipeline, Gemini review).
mkdir -p ~/tools
rsync -av ed@192.168.1.38:/home/ed/tools/code-review ~/tools/

# Gemini CLI auth (contains credentials — trusted LAN only).
rsync -av ed@192.168.1.38:/home/ed/.gemini/ ~/.gemini/

# Verify.
ls ~/.claude/projects/-home-ed-silicritter/memory/MEMORY.md \
   ~/tools/code-review/pre-commit-hook.sh \
   ~/.gemini/oauth_creds.json \
   ~/.claude/CLAUDE.md

# Then in silicritter/:
cd silicritter
python3.12 -m venv .venv
source .venv/bin/activate
pip install -e .
pip install 'jax[cuda12]'  # adjust for the second laptop's CUDA
ln -sf ~/tools/code-review/pre-commit-hook.sh .git/hooks/pre-commit

# Smoke test: tests should be 125 passed, 100% branch coverage.
.venv/bin/pytest tests/ -q
```

## Pointers for tomorrow-Claude

When the next session picks up on the second laptop, the key memory
files to read first are:

- `~/.claude/projects/-home-ed-silicritter/memory/MEMORY.md` (index)
- `~/.claude/projects/-home-ed-silicritter/memory/project_revalidation_plan.md` (the paused plan)
- `~/.claude/projects/-home-ed-silicritter/memory/feedback_runtime_not_precious.md` (Ed's compute-vs-review-time framing)

The resumption sequence is:

1. Read this file.
2. Read the memory files above.
3. Fix the raster-retention OOM in step17 (and step16) — small, contained.
4. Design the revalidation batch (blocks 9–13) at N=100/500.
5. Launch. Review once, not per-block.
